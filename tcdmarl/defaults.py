# Number of separate trials to run the algorithm for
DEFAULT_NUM_SEPARATE_TRIALS: int = 10

# Number of times to multiply the step unit factor (~= number of episodes)
DEFAULT_STEP_UNIT_FACTOR: int = 100
